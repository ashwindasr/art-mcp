# Konflux DB related vars
BIGQUERY_PROJECT_ID=openshift-art
BIGQUERY_DATASET_ID=events
BIGQUERY_TABLE_ID=builds

# Google Cloud Authentication
# Option 1: Service Account Key (not recommended for production)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Option 2: Use gcloud auth (recommended for development)
# Run: gcloud auth application-default login